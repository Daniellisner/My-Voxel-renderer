"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spread = (this && this.__spread) || function () {
    for (var ar = [], i = 0; i < arguments.length; i++) ar = ar.concat(__read(arguments[i]));
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.logError = exports.getProperties = exports.collectDependences = exports.mergeOptions = exports.genClassKey = exports.getClassName = exports.hashCode = void 0;
var persist_1 = require("../middleware/persist");
exports.hashCode = function (str) {
    return str.split('').reduce(function (a, b) {
        a = (a << 5) - a + b.charCodeAt(0);
        return a & a;
    }, 0);
};
exports.getClassName = function (clazz) {
    var classStr = clazz.toString();
    var _a = __read(classStr.match(/class\s+(\w+).*{/) || [], 2), name1 = _a[1];
    var _b = __read(classStr.match(/function\s+([^(]*)\(/) || [], 2), name2 = _b[1];
    return name1 || name2 || '';
};
exports.genClassKey = function (InjectedStoreClass, identification) {
    if (identification === void 0) { identification = ''; }
    var keyPrefix = 'MATROX';
    var className = exports.getClassName(InjectedStoreClass);
    var classHaseCode = exports.hashCode(InjectedStoreClass.toString());
    var key = keyPrefix + "@" + className + "@" + classHaseCode + "@" + identification;
    return { key: key, className: className, classHaseCode: classHaseCode, keyPrefix: keyPrefix };
};
exports.mergeOptions = function (options, globalOptions) {
    var _a, _b, _c, _d;
    var newOptions = __assign(__assign({}, globalOptions), options);
    newOptions.middlewares = __spread(new Set(__spread(((globalOptions === null || globalOptions === void 0 ? void 0 : globalOptions.middlewares) || []), ((options === null || options === void 0 ? void 0 : options.middlewares) || []))));
    if ((newOptions === null || newOptions === void 0 ? void 0 : newOptions.persist) && !((_a = newOptions === null || newOptions === void 0 ? void 0 : newOptions.middlewares) === null || _a === void 0 ? void 0 : _a.includes(persist_1.default))) {
        (_b = newOptions === null || newOptions === void 0 ? void 0 : newOptions.middlewares) === null || _b === void 0 ? void 0 : _b.unshift(persist_1.default);
    }
    if ((_c = options === null || options === void 0 ? void 0 : options.ignoreMiddlewares) === null || _c === void 0 ? void 0 : _c.length) {
        newOptions.middlewares = (_d = newOptions.middlewares) === null || _d === void 0 ? void 0 : _d.filter(function (item) { var _a; return !((_a = options === null || options === void 0 ? void 0 : options.ignoreMiddlewares) === null || _a === void 0 ? void 0 : _a.includes(item)); });
    }
    return newOptions;
};
exports.collectDependences = function (instance, liseners, ignoredProps) {
    if (ignoredProps === void 0) { ignoredProps = []; }
    return new Proxy(instance, {
        get: function (target, key) {
            var result = target[key];
            if (typeof key !== 'symbol' && typeof result !== 'function' && !ignoredProps.includes(key)) {
                liseners.forEach(function (item) {
                    item.watchedProps.add(key);
                });
            }
            return result;
        },
        set: function (target, key, value) {
            if (typeof key === 'symbol') {
                return Reflect.set(target, key, value);
            }
            exports.logError('store property is readonly.');
            return false;
        }
    });
};
exports.getProperties = function (target, ignoredProps) {
    return Object.keys(target).reduce(function (res, key) {
        var result = target[key];
        if (result === undefined) {
            exports.logError("please define \"" + key + "\" before using.");
            return;
        }
        // tslint:disable-next-line: strict-type-predicates
        if (!ignoredProps.includes(key) && typeof key !== 'symbol' && typeof result !== 'function') {
            res[key] = result;
        }
        return res;
    }, {});
};
exports.logError = function (msg) {
    var message = 'Matrox: ' + msg;
    if (process.env.NODE_ENV === 'production') {
        console.error(message);
    }
    else {
        throw Error(message);
    }
};
//# sourceMappingURL=utils.js.map