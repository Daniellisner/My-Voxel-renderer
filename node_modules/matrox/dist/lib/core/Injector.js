"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spread = (this && this.__spread) || function () {
    for (var ar = [], i = 0; i < arguments.length; i++) ar = ar.concat(__read(arguments[i]));
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getInjector = void 0;
var utils_1 = require("./utils");
var StoreBase_1 = require("../api/StoreBase");
var StoreBase_2 = require("../types/StoreBase");
var globalConfig_1 = require("../api/globalConfig");
var applyMiddleware_1 = require("./applyMiddleware");
var cachedInjector;
exports.getInjector = function () {
    return cachedInjector || (cachedInjector = Injector.newInstance());
};
var Injector = /** @class */ (function () {
    function Injector() {
        this.appContainer = new Map();
    }
    Injector.newInstance = function () {
        return new Injector();
    };
    Injector.prototype.clear = function () {
        this.appContainer.clear();
    };
    Injector.prototype.subscribe = function (InjectedStoreClass, liseners, compType, identification) {
        if (identification === void 0) { identification = ''; }
        var instance = this.get(InjectedStoreClass, liseners, identification);
        var instanceLiseners = instance[StoreBase_1._meta].liseners || [];
        liseners.forEach(function (item) {
            if (!instanceLiseners.find(function (it) { return it.self === item.self; })) {
                if (compType === StoreBase_2.CompType.FUNCTION) {
                    instance[StoreBase_1._meta].liseners = __spread(liseners, instanceLiseners);
                }
                else {
                    instance[StoreBase_1._meta].liseners = __spread(instanceLiseners, liseners);
                }
            }
        });
        return function () {
            instance[StoreBase_1._meta].liseners = instance[StoreBase_1._meta].liseners.filter(function (item) { return !liseners.find(function (it) { return it.forceUpdate === item.forceUpdate; }); });
        };
    };
    Injector.prototype.get = function (InjectedStoreClass, liseners, identification) {
        var _a;
        if (identification === void 0) { identification = ''; }
        var _b = InjectedStoreClass[StoreBase_1._meta], options1 = _b.options, _c = _b.ignoredProps, ignoredProps = _c === void 0 ? [] : _c;
        var options = utils_1.mergeOptions(options1, globalConfig_1.globalOptions);
        var instance;
        var container = this.appContainer;
        var _d = utils_1.genClassKey(InjectedStoreClass, identification), key = _d.key, className = _d.className;
        instance = container.get(key);
        if (!instance) {
            instance = new InjectedStoreClass();
            var initialValues = utils_1.getProperties(instance, []);
            instance[StoreBase_1._meta] = __assign(__assign({}, instance[StoreBase_1._meta]), { options: options,
                ignoredProps: ignoredProps,
                key: key, storeName: className, initialValues: initialValues });
            if ((_a = options === null || options === void 0 ? void 0 : options.middlewares) === null || _a === void 0 ? void 0 : _a.length) {
                applyMiddleware_1.default.apply(void 0, __spread((options.middlewares || [])))(instance);
            }
            container.set(key, instance);
        }
        var instanceProxy = utils_1.collectDependences(instance, liseners, ignoredProps);
        return instanceProxy;
    };
    return Injector;
}());
//# sourceMappingURL=Injector.js.map