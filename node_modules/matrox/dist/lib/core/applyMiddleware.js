"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spread = (this && this.__spread) || function () {
    for (var ar = [], i = 0; i < arguments.length; i++) ar = ar.concat(__read(arguments[i]));
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
var compose_1 = require("./compose");
var StoreBase_1 = require("../api/StoreBase");
var utils_1 = require("./utils");
var applyMiddleware = function () {
    var middlewares = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        middlewares[_i] = arguments[_i];
    }
    return function (store) {
        var dispatch = function () {
            utils_1.logError("dispatching while constructing your middleware is not allowed. Other middleware would not be applied to this dispatch.");
        };
        var _a = store[StoreBase_1._meta], liseners = _a.liseners, rest = __rest(_a, ["liseners"]);
        var storeAPI = {
            meta: rest,
            updatePropsWithoutRender: store[StoreBase_1._updatePropsWithoutRender],
            getState: store.getState,
            dispatch: store.setProps
        };
        var chain = middlewares.map(function (middleware) { return middleware(storeAPI); });
        dispatch = compose_1.default.apply(void 0, __spread(chain))(store.setProps);
        store.setProps = dispatch;
        return __assign(__assign({}, store), { dispatch: dispatch });
    };
};
exports.default = applyMiddleware;
//# sourceMappingURL=applyMiddleware.js.map