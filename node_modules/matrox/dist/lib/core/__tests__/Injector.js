"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
var Injector_1 = require("../Injector");
var __1 = require("../..");
var StoreBase_1 = require("../../api/StoreBase");
var StoreBase_2 = require("../../types/StoreBase");
var store_1 = require("../../api/store");
describe('Injector', function () {
    var injector = Injector_1.getInjector();
    var injectorAny = injector;
    var A = /** @class */ (function (_super) {
        __extends(A, _super);
        function A() {
            var _this = _super !== null && _super.apply(this, arguments) || this;
            _this.name = '1';
            return _this;
        }
        A = __decorate([
            store_1.default()
        ], A);
        return A;
    }(__1.StoreBase));
    var B = /** @class */ (function (_super) {
        __extends(B, _super);
        function B() {
            var _this = _super !== null && _super.apply(this, arguments) || this;
            _this.age = 0;
            return _this;
        }
        B = __decorate([
            store_1.default()
        ], B);
        return B;
    }(__1.StoreBase));
    beforeEach(function () {
        injectorAny.clear();
    });
    test('the get method of Injector class should get a same instance for a same class', function () {
        var injector1 = Injector_1.getInjector();
        expect(injector).toBe(injector1);
    });
    test('the get method of Injector class should get a same instance for a same class', function () {
        var a = injector.get(A, [], '');
        var a1 = injector.get(A, []);
        expect(a).toStrictEqual(a1);
        expect(injectorAny.appContainer.size).toBe(1);
    });
    test("the identification parameter of get method of Injector class should control the initiation of instance for session store", function () {
        var b = injector.get(B, [], 1);
        var b1 = injector.get(B, [], 2);
        expect(b1).not.toBe(b);
    });
    test("the subscribe method of Injector class should subscribe the store instance for comp and return a unsubscribe function", function () {
        var arr = [A];
        arr.forEach(function (Clazz) {
            var forceUpdate = function () { return undefined; };
            var forceUpdate1 = function () { return undefined; };
            var _a = __read([{}, StoreBase_2.CompType.FUNCTION, StoreBase_2.Role.COMPONENT], 3), self = _a[0], compType = _a[1], role = _a[2];
            var liseners = [
                { forceUpdate: forceUpdate, self: self, watchedProps: new Set(), compType: compType, role: role }
            ];
            var liseners1 = [
                { forceUpdate: forceUpdate1, self: self, watchedProps: new Set(), compType: compType, role: role }
            ];
            var unsubscribe = injector.subscribe(Clazz, liseners, compType);
            // 未实例化则实例化
            expect(injectorAny.appContainer.size).toBe(1);
            var a = injector.get(Clazz, liseners);
            expect(a[StoreBase_1._meta].liseners.length).toBe(1);
            // 重复注册
            var unsubscribe1 = injector.subscribe(Clazz, liseners1, compType);
            expect(a[StoreBase_1._meta].liseners.length).toBe(1);
            unsubscribe1();
            expect(a[StoreBase_1._meta].liseners.length).toBe(1);
            unsubscribe();
            expect(a[StoreBase_1._meta].liseners.length).toBe(0);
        });
    });
});
//# sourceMappingURL=Injector.js.map