"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spread = (this && this.__spread) || function () {
    for (var ar = [], i = 0; i < arguments.length; i++) ar = ar.concat(__read(arguments[i]));
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
var utils_1 = require("../utils");
var __1 = require("../..");
var persist_1 = require("../../middleware/persist");
var StoreBase_1 = require("../../types/StoreBase");
describe('Injector utils', function () {
    var A = /** @class */ (function (_super) {
        __extends(A, _super);
        function A() {
            return _super !== null && _super.apply(this, arguments) || this;
        }
        return A;
    }(__1.StoreBase));
    var B = /** @class */ (function (_super) {
        __extends(B, _super);
        function B() {
            return _super !== null && _super.apply(this, arguments) || this;
        }
        return B;
    }(__1.StoreBase));
    var A1 = A;
    test('function hashCode should generate a unique hash code', function () {
        var a = utils_1.hashCode(A.toString());
        var a1 = utils_1.hashCode(A1.toString());
        var b = utils_1.hashCode(B.toString());
        expect(a).not.toBe(b);
        expect(a1).toBe(a);
    });
    test('function getClassName should get class name', function () {
        var a = utils_1.getClassName(A);
        var a1 = utils_1.getClassName(A1);
        var b = utils_1.getClassName(B);
        expect(a).toBe("A");
        expect(a1).toBe("A");
        expect(b).toBe("B");
    });
    test('function genClassKey should generate a unique key for class', function () {
        var a = utils_1.genClassKey(A);
        var a1 = utils_1.genClassKey(A1);
        var b = utils_1.genClassKey(B);
        expect(a.key).toBe(a1.key);
        expect(b.key).not.toBe(a.key);
    });
    test('function mergeOptions should merge options and globalOptions', function () {
        var options = {};
        var globalOptions = { persist: true };
        var newOptions = utils_1.mergeOptions(options, globalOptions);
        expect(newOptions.persist).toBeTruthy();
        expect(newOptions.middlewares).toEqual([persist_1.default]);
        var options1 = { ignoreMiddlewares: [persist_1.default] };
        var globalOptions1 = { middlewares: [persist_1.default] };
        var newOptions1 = utils_1.mergeOptions(options1, globalOptions1);
        expect(newOptions1.middlewares).toEqual([]);
    });
    test('function collectDependences should return a proxy instance watching some properties', function () {
        var _a;
        var _money = Symbol('_money');
        var A = /** @class */ (function (_super) {
            __extends(A, _super);
            function A() {
                var _this = _super !== null && _super.apply(this, arguments) || this;
                _this[_a] = 0;
                _this.name = 'jack';
                _this.sex = 'man';
                _this.age = 18;
                _this.eat = function () { return undefined; };
                return _this;
            }
            return A;
        }(__1.StoreBase));
        _a = _money;
        var a = new A();
        var watchedProps = new Set();
        var liseners = [
            {
                forceUpdate: function () {
                    return;
                },
                self: {},
                watchedProps: watchedProps,
                compType: StoreBase_1.CompType.FUNCTION,
                role: StoreBase_1.Role.COMPONENT
            }
        ];
        var proxyInstance = utils_1.collectDependences(a, liseners, ['age']);
        expect(proxyInstance).toBeDefined();
        expect(proxyInstance).toBeInstanceOf(A);
        // 依赖收集
        var _ = proxyInstance.name;
        expect(__spread(watchedProps)).toEqual(['name']);
        // 依赖收集
        var _1 = proxyInstance.sex;
        expect(__spread(watchedProps)).toEqual(['name', 'sex']);
        // 避免依赖收集
        var _3 = proxyInstance.age;
        expect(__spread(watchedProps)).toEqual(['name', 'sex']);
        var _4 = proxyInstance.eat;
        expect(__spread(watchedProps)).toEqual(['name', 'sex']);
        var _5 = proxyInstance[_money];
        expect(__spread(watchedProps)).toEqual(['name', 'sex']);
        // readonly
        expect(function () { return (proxyInstance[_money] = 2); }).not.toThrow();
        expect(function () { return (proxyInstance.name = 'stupid'); }).toThrow();
        expect(function () { return (proxyInstance.eat = function () { return undefined; }); }).toThrow();
    });
    test('function getProperties should get properties of an object', function () {
        var _a;
        var rs = utils_1.getProperties((_a = { a: function () { return undefined; }, b: '1' }, _a[Symbol('c')] = 'c', _a), []);
        expect(rs).toEqual({ b: '1' });
    });
    test('function logError should log error message in production environment instead of throw error', function () {
        var backup = process.env.NODE_ENV;
        process.env.NODE_ENV = 'development';
        expect(function () { return utils_1.logError('test'); }).toThrow();
        process.env.NODE_ENV = 'production';
        expect(function () { return utils_1.logError('test'); }).not.toThrow();
        process.env.NODE_ENV = backup;
    });
});
//# sourceMappingURL=utils.js.map