import StoreBase from '../api/StoreBase';
import { Constructor } from '../types/store';
import { Lisener, CompType } from '../types/StoreBase';
export declare const getInjector: () => Injector;
declare class Injector {
    private readonly appContainer;
    static newInstance(): Injector;
    clear(): void;
    subscribe<T extends StoreBase<T>>(InjectedStoreClass: Constructor<T>, liseners: Lisener[], compType: CompType, identification?: string | number): () => void;
    get<T extends StoreBase<T>>(InjectedStoreClass: Constructor<T>, liseners: Lisener[], identification?: string | number): T;
}
export {};
