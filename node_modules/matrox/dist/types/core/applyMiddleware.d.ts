import StoreBase from '../api/StoreBase';
import { MiddleWare } from '../types/middleware';
declare const applyMiddleware: (...middlewares: MiddleWare[]) => <T extends StoreBase<any, string>>(store: T) => T & {
    dispatch: any;
};
export default applyMiddleware;
