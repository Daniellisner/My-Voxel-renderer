(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports, require('react')) :
    typeof define === 'function' && define.amd ? define(['exports', 'react'], factory) :
    (factory((global.matrox = {}),global.React));
}(this, (function (exports,react) { 'use strict';

    /*! *****************************************************************************
    Copyright (c) Microsoft Corporation. All rights reserved.
    Licensed under the Apache License, Version 2.0 (the "License"); you may not use
    this file except in compliance with the License. You may obtain a copy of the
    License at http://www.apache.org/licenses/LICENSE-2.0

    THIS CODE IS PROVIDED ON AN *AS IS* BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
    KIND, EITHER EXPRESS OR IMPLIED, INCLUDING WITHOUT LIMITATION ANY IMPLIED
    WARRANTIES OR CONDITIONS OF TITLE, FITNESS FOR A PARTICULAR PURPOSE,
    MERCHANTABLITY OR NON-INFRINGEMENT.

    See the Apache Version 2.0 License for specific language governing permissions
    and limitations under the License.
    ***************************************************************************** */

    var __assign = function() {
        __assign = Object.assign || function __assign(t) {
            for (var s, i = 1, n = arguments.length; i < n; i++) {
                s = arguments[i];
                for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
            }
            return t;
        };
        return __assign.apply(this, arguments);
    };

    function __rest(s, e) {
        var t = {};
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
            t[p] = s[p];
        if (s != null && typeof Object.getOwnPropertySymbols === "function")
            for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0)
                t[p[i]] = s[p[i]];
        return t;
    }

    function __awaiter(thisArg, _arguments, P, generator) {
        return new (P || (P = Promise))(function (resolve, reject) {
            function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
            function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
            function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
            step((generator = generator.apply(thisArg, _arguments || [])).next());
        });
    }

    function __generator(thisArg, body) {
        var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
        return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
        function verb(n) { return function (v) { return step([n, v]); }; }
        function step(op) {
            if (f) throw new TypeError("Generator is already executing.");
            while (_) try {
                if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
                if (y = 0, t) op = [op[0] & 2, t.value];
                switch (op[0]) {
                    case 0: case 1: t = op; break;
                    case 4: _.label++; return { value: op[1], done: false };
                    case 5: _.label++; y = op[1]; op = [0]; continue;
                    case 7: op = _.ops.pop(); _.trys.pop(); continue;
                    default:
                        if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                        if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                        if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                        if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                        if (t[2]) _.ops.pop();
                        _.trys.pop(); continue;
                }
                op = body.call(thisArg, _);
            } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
            if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
        }
    }

    function __read(o, n) {
        var m = typeof Symbol === "function" && o[Symbol.iterator];
        if (!m) return o;
        var i = m.call(o), r, ar = [], e;
        try {
            while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
        }
        catch (error) { e = { error: error }; }
        finally {
            try {
                if (r && !r.done && (m = i["return"])) m.call(i);
            }
            finally { if (e) throw e.error; }
        }
        return ar;
    }

    function __spread() {
        for (var ar = [], i = 0; i < arguments.length; i++)
            ar = ar.concat(__read(arguments[i]));
        return ar;
    }

    var persist = function (store) {
        var key = (store.meta || {}).key;
        var state = JSON.parse(localStorage.getItem(key) || '{}');
        store.updatePropsWithoutRender(state);
        return function (dispatch) { return function (action) { return __awaiter(void 0, void 0, void 0, function () {
            var state;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: return [4 /*yield*/, dispatch(action)];
                    case 1:
                        _a.sent();
                        state = store.getState();
                        localStorage.setItem(key, JSON.stringify(state));
                        return [2 /*return*/];
                }
            });
        }); }; };
    };

    var hashCode = function (str) {
        return str.split('').reduce(function (a, b) {
            a = (a << 5) - a + b.charCodeAt(0);
            return a & a;
        }, 0);
    };
    var getClassName = function (clazz) {
        var classStr = clazz.toString();
        var _a = __read(classStr.match(/class\s+(\w+).*{/) || [], 2), name1 = _a[1];
        var _b = __read(classStr.match(/function\s+([^(]*)\(/) || [], 2), name2 = _b[1];
        return name1 || name2 || '';
    };
    var genClassKey = function (InjectedStoreClass, identification) {
        if (identification === void 0) { identification = ''; }
        var keyPrefix = 'MATROX';
        var className = getClassName(InjectedStoreClass);
        var classHaseCode = hashCode(InjectedStoreClass.toString());
        var key = keyPrefix + "@" + className + "@" + classHaseCode + "@" + identification;
        return { key: key, className: className, classHaseCode: classHaseCode, keyPrefix: keyPrefix };
    };
    var mergeOptions = function (options, globalOptions) {
        var _a, _b, _c, _d;
        var newOptions = __assign(__assign({}, globalOptions), options);
        newOptions.middlewares = __spread(new Set(__spread(((globalOptions === null || globalOptions === void 0 ? void 0 : globalOptions.middlewares) || []), ((options === null || options === void 0 ? void 0 : options.middlewares) || []))));
        if ((newOptions === null || newOptions === void 0 ? void 0 : newOptions.persist) && !((_a = newOptions === null || newOptions === void 0 ? void 0 : newOptions.middlewares) === null || _a === void 0 ? void 0 : _a.includes(persist))) {
            (_b = newOptions === null || newOptions === void 0 ? void 0 : newOptions.middlewares) === null || _b === void 0 ? void 0 : _b.unshift(persist);
        }
        if ((_c = options === null || options === void 0 ? void 0 : options.ignoreMiddlewares) === null || _c === void 0 ? void 0 : _c.length) {
            newOptions.middlewares = (_d = newOptions.middlewares) === null || _d === void 0 ? void 0 : _d.filter(function (item) { var _a; return !((_a = options === null || options === void 0 ? void 0 : options.ignoreMiddlewares) === null || _a === void 0 ? void 0 : _a.includes(item)); });
        }
        return newOptions;
    };
    var collectDependences = function (instance, liseners, ignoredProps) {
        if (ignoredProps === void 0) { ignoredProps = []; }
        return new Proxy(instance, {
            get: function (target, key) {
                var result = target[key];
                if (typeof key !== 'symbol' && typeof result !== 'function' && !ignoredProps.includes(key)) {
                    liseners.forEach(function (item) {
                        item.watchedProps.add(key);
                    });
                }
                return result;
            },
            set: function (target, key, value) {
                if (typeof key === 'symbol') {
                    return Reflect.set(target, key, value);
                }
                logError('store property is readonly.');
                return false;
            }
        });
    };
    var getProperties = function (target, ignoredProps) {
        return Object.keys(target).reduce(function (res, key) {
            var result = target[key];
            if (result === undefined) {
                logError("please define \"" + key + "\" before using.");
                return;
            }
            // tslint:disable-next-line: strict-type-predicates
            if (!ignoredProps.includes(key) && typeof key !== 'symbol' && typeof result !== 'function') {
                res[key] = result;
            }
            return res;
        }, {});
    };
    var logError = function (msg) {
        var message = 'Matrox: ' + msg;
        if (process.env.NODE_ENV === 'production') {
            console.error(message);
        }
        else {
            throw Error(message);
        }
    };

    var Role;
    (function (Role) {
        Role["STORE"] = "STORE";
        Role["COMPONENT"] = "COMPONENT";
    })(Role || (Role = {}));
    var CompType;
    (function (CompType) {
        CompType["FUNCTION"] = "FUNCTION";
        CompType["CLASS"] = "CLASS";
    })(CompType || (CompType = {}));

    var allLiseners = [];
    var isBatchingUpdate = false;
    var reduceLisners = function (liseners, reducedLiseners) {
        var tmpLisener = __spread(reducedLiseners);
        liseners
            .map(function (item) { var _a; return (item.role === Role.STORE ? ((_a = item.self[_meta]) === null || _a === void 0 ? void 0 : _a.liseners) || [] : item); })
            .flat(1)
            .forEach(function (item) {
            if (!tmpLisener.find(function (it) { return it.self === item.self; }) && item.forceUpdate) {
                tmpLisener.push(item);
            }
        });
        return tmpLisener;
    };
    var directUpdate = function (liseners) {
        allLiseners = reduceLisners(liseners, allLiseners);
        walkUpdate();
    };
    var batchingUpdate = function (liseners) { return __awaiter(void 0, void 0, void 0, function () {
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    isBatchingUpdate = true;
                    allLiseners = reduceLisners(liseners, allLiseners);
                    return [4 /*yield*/, Promise.resolve()];
                case 1:
                    _a.sent();
                    if (!isBatchingUpdate) {
                        return [2 /*return*/];
                    }
                    isBatchingUpdate = false;
                    walkUpdate();
                    return [2 /*return*/];
            }
        });
    }); };
    var walkUpdate = function () {
        allLiseners.forEach(function (item) { var _a; return (_a = item === null || item === void 0 ? void 0 : item.forceUpdate) === null || _a === void 0 ? void 0 : _a.call(item); });
        allLiseners = [];
    };
    var getEffectiveLiseners = function (liseners, updateObject) {
        var effectiveLiseners = liseners.filter(function (item) {
            return __spread(item.watchedProps).some(function (it) {
                return Object.keys(updateObject).includes(it);
            });
        });
        return effectiveLiseners;
    };
    var reduceUpdateObject = function (target, updateObject) {
        return Object.keys(updateObject).reduce(function (res, key) {
            if (target[key] === undefined) {
                logError("target don't exist the '" + key + "', please define it before using.");
                return;
            }
            if (typeof updateObject[key] !== typeof target[key]) {
                logError("expected " + typeof target[key] + " but " + typeof updateObject[key] + " in field " + key + ".");
                return;
            }
            if (updateObject[key] !== target[key]) {
                res[key] = updateObject[key];
            }
            return res;
        }, {});
    };
    var updateTarget = function (target, updateObject) {
        Object.keys(updateObject).forEach(function (key) {
            if (!key) {
                throw new Error('Unuseful object!');
            }
            var value = updateObject[key];
            if (typeof value === 'function') {
                logError("forbid reset method of class.");
                return;
            }
            Reflect.set(target, key, value);
        });
    };
    var compateAction = function (payload, type) {
        // payload is "action" or "payload"
        if (typeof payload === 'function') {
            return { payload: payload, type: type };
        }
        // payload is "action"
        if (payload.type !== undefined && payload.payload !== undefined) {
            return payload;
        }
        // payload is "payload"
        return { payload: payload, type: type };
    };

    var _a, _b, _c, _d, _e, _f;
    var _isBatchingUpdate = Symbol("_isBatchingUpdate");
    var _actionsMergedQueue = Symbol("_actionsMergedQueue");
    var _dispatchAction = Symbol("_dispatchAction");
    var _mergeAction = Symbol("_mergeAction");
    var _updatePropsWithoutRender = Symbol("_updatePropsWithoutRender");
    var _meta = Symbol("_meta");
    /**
     * store must extends StoreBase
     *
     * @example
     * ```ts
     * import { StoreBase } from 'matrox'
     * class Store extends StoreBase<Store> {
     * }
     *
     * ```
     *
     * @see https://github.com/anthhub/matrox#store
     */
    var StoreBase = /** @class */ (function () {
        function StoreBase() {
            var _this = this;
            this[_a] = false;
            this[_b] = [];
            this[_c] = {
                liseners: [],
                options: {},
                key: '',
                storeName: '',
                ignoredProps: [],
                initialValues: {}
            };
            this[_d] = function (action) { return __awaiter(_this, void 0, void 0, function () {
                var _g, payload, type, updateObject;
                return __generator(this, function (_h) {
                    if (typeof action === 'function') {
                        action = action(this);
                    }
                    if (typeof action.payload === 'function') {
                        action.payload = action.payload(this);
                    }
                    _g = compateAction(action.payload, action.type), payload = _g.payload, type = _g.type;
                    updateObject = this[_updatePropsWithoutRender](payload);
                    return [2 /*return*/, batchingUpdate(getEffectiveLiseners(this[_meta].liseners, updateObject))];
                });
            }); };
            this[_e] = function () { return __awaiter(_this, void 0, void 0, function () {
                var self, propsMergedObject;
                var _this = this;
                return __generator(this, function (_g) {
                    switch (_g.label) {
                        case 0:
                            self = this;
                            propsMergedObject = this[_actionsMergedQueue].reduce(function (res, cur, index) {
                                if (typeof cur === 'function') {
                                    cur = cur(_this);
                                }
                                if (typeof cur.payload === 'function') {
                                    cur.payload = cur.payload(_this);
                                }
                                var _g = compateAction(cur.payload, cur.type), payload = _g.payload, type = _g.type;
                                if (typeof payload === 'function' && index !== 0) {
                                    _this[_updatePropsWithoutRender](payload);
                                }
                                var rs = typeof payload === 'function' ? payload(self) : payload;
                                var payload1 = __assign(__assign({}, res), rs);
                                if (typeof payload === 'function' && index !== _this[_actionsMergedQueue].length - 1) {
                                    _this[_updatePropsWithoutRender](payload1);
                                }
                                return payload1;
                            }, {});
                            this[_actionsMergedQueue] = [];
                            return [4 /*yield*/, this[_dispatchAction]({ payload: propsMergedObject, type: undefined })];
                        case 1:
                            _g.sent();
                            return [2 /*return*/];
                    }
                });
            }); };
            this[_f] = function (payload) {
                var self = _this;
                var plainPayload = typeof payload === 'function' ? payload(self) : payload;
                var updateObject;
                updateObject = reduceUpdateObject(_this, plainPayload);
                updateTarget(_this, updateObject);
                return updateObject;
            };
            /**
             * force render all components or stores using this store
             *
             * @example
             * ```ts
             * store.forceUpdate()
             * ```
             *
             * @see https://github.com/anthhub/matrox#forceUpdate
             */
            this.forceUpdate = function () {
                return batchingUpdate(_this[_meta].liseners);
            };
            /**
             *  directly force render all components or stores using this store
             *
             * @example
             * ```ts
             * store.forceUpdate()
             * ```
             *
             * @see https://github.com/anthhub/matrox#forceUpdateSync
             */
            this.forceUpdateSync = function () {
                return directUpdate(_this[_meta].liseners);
            };
            /**
             * reset store properties and then render all components or stores using this store
             *
             * @example
             * ```ts
             * store.resetStore()
             * ```
             *
             * @see https://github.com/anthhub/matrox#resetStore
             */
            this.resetStore = function () {
                var initialValues = _this[_meta].initialValues;
                return _this.setProps(function () { return initialValues; });
            };
            /**
             * merge action and batching update store or component using this store
             *
             * @param  {Payload<T>|Action<T} payload action or payload of action
             * @param  {U} type? type of action
             *
             * @example
             * ```ts
             * store.setProps({ name:'marry' })
             * ```
             *
             * @see https://github.com/anthhub/matrox#setProps
             */
            this.setProps = function (payload, type) { return __awaiter(_this, void 0, void 0, function () {
                var action;
                return __generator(this, function (_g) {
                    switch (_g.label) {
                        case 0:
                            this[_isBatchingUpdate] = true;
                            action = compateAction(payload, type);
                            this[_actionsMergedQueue].push(action);
                            return [4 /*yield*/, Promise.resolve()];
                        case 1:
                            _g.sent();
                            if (!this[_isBatchingUpdate]) {
                                return [2 /*return*/];
                            }
                            this[_isBatchingUpdate] = false;
                            return [4 /*yield*/, this[_mergeAction]()];
                        case 2:
                            _g.sent();
                            return [2 /*return*/];
                    }
                });
            }); };
            /**
             * get store static state (watched props)
             *
             *
             * @example
             * ```ts
             * store.getState()
             * ```
             *
             * @see https://github.com/anthhub/matrox#getState
             */
            this.getState = function () {
                var ignoredProps = _this[_meta].ignoredProps || [];
                var state = getProperties(_this, ignoredProps);
                return JSON.parse(JSON.stringify(state));
            };
        }
        return StoreBase;
    }());
    _a = _isBatchingUpdate, _b = _actionsMergedQueue, _c = _meta, _d = _dispatchAction, _e = _mergeAction, _f = _updatePropsWithoutRender;

    var globalOptions;
    /**
     * config add apply options for all store
     *
     * @param  {GlobalOptions} options global options
     *
     * @example
     * ```ts
     * import { GlobalOptions } from 'matrox'
     * globalConfig({ persist : true })
     * ```
     *
     * @see https://github.com/anthhub/matrox#globalConfig
     */
    var globalConfig = function (options) {
        globalOptions = options;
    };

    var compose = function () {
        var funcs = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            funcs[_i] = arguments[_i];
        }
        if (funcs.length === 0) {
            return function (arg) { return arg; };
        }
        if (funcs.length === 1) {
            return funcs[0];
        }
        return funcs.reduce(function (a, b) { return function () {
            var args = [];
            for (var _i = 0; _i < arguments.length; _i++) {
                args[_i] = arguments[_i];
            }
            return a(b.apply(void 0, __spread(args)));
        }; });
    };

    var applyMiddleware = function () {
        var middlewares = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            middlewares[_i] = arguments[_i];
        }
        return function (store) {
            var dispatch = function () {
                logError("dispatching while constructing your middleware is not allowed. Other middleware would not be applied to this dispatch.");
            };
            var _a = store[_meta], liseners = _a.liseners, rest = __rest(_a, ["liseners"]);
            var storeAPI = {
                meta: rest,
                updatePropsWithoutRender: store[_updatePropsWithoutRender],
                getState: store.getState,
                dispatch: store.setProps
            };
            var chain = middlewares.map(function (middleware) { return middleware(storeAPI); });
            dispatch = compose.apply(void 0, __spread(chain))(store.setProps);
            store.setProps = dispatch;
            return __assign(__assign({}, store), { dispatch: dispatch });
        };
    };

    var cachedInjector;
    var getInjector = function () {
        return cachedInjector || (cachedInjector = Injector.newInstance());
    };
    var Injector = /** @class */ (function () {
        function Injector() {
            this.appContainer = new Map();
        }
        Injector.newInstance = function () {
            return new Injector();
        };
        Injector.prototype.clear = function () {
            this.appContainer.clear();
        };
        Injector.prototype.subscribe = function (InjectedStoreClass, liseners, compType, identification) {
            if (identification === void 0) { identification = ''; }
            var instance = this.get(InjectedStoreClass, liseners, identification);
            var instanceLiseners = instance[_meta].liseners || [];
            liseners.forEach(function (item) {
                if (!instanceLiseners.find(function (it) { return it.self === item.self; })) {
                    if (compType === CompType.FUNCTION) {
                        instance[_meta].liseners = __spread(liseners, instanceLiseners);
                    }
                    else {
                        instance[_meta].liseners = __spread(instanceLiseners, liseners);
                    }
                }
            });
            return function () {
                instance[_meta].liseners = instance[_meta].liseners.filter(function (item) { return !liseners.find(function (it) { return it.forceUpdate === item.forceUpdate; }); });
            };
        };
        Injector.prototype.get = function (InjectedStoreClass, liseners, identification) {
            var _a;
            if (identification === void 0) { identification = ''; }
            var _b = InjectedStoreClass[_meta], options1 = _b.options, _c = _b.ignoredProps, ignoredProps = _c === void 0 ? [] : _c;
            var options = mergeOptions(options1, globalOptions);
            var instance;
            var container = this.appContainer;
            var _d = genClassKey(InjectedStoreClass, identification), key = _d.key, className = _d.className;
            instance = container.get(key);
            if (!instance) {
                instance = new InjectedStoreClass();
                var initialValues = getProperties(instance, []);
                instance[_meta] = __assign(__assign({}, instance[_meta]), { options: options,
                    ignoredProps: ignoredProps,
                    key: key, storeName: className, initialValues: initialValues });
                if ((_a = options === null || options === void 0 ? void 0 : options.middlewares) === null || _a === void 0 ? void 0 : _a.length) {
                    applyMiddleware.apply(void 0, __spread((options.middlewares || [])))(instance);
                }
                container.set(key, instance);
            }
            var instanceProxy = collectDependences(instance, liseners, ignoredProps);
            return instanceProxy;
        };
        return Injector;
    }());

    var injector = getInjector();
    var getInjection = function (InjectedStoreClass, identification) {
        return injector.get(InjectedStoreClass, [], identification);
    };

    var useThis = function (args) {
        var comp = react.useRef({ args: args });
        return comp.current;
    };

    var useForceUpdate = function () {
        var _a = __read(react.useState(0), 2), _forceUpdate = _a[1];
        return react.useCallback(function () { return _forceUpdate(function (n) { return n + 1; }); }, []);
    };

    var injector$1 = getInjector();
    var useInjection = function (InjectedStoreClass, identification) {
        var self = useThis();
        var forceUpdate = useForceUpdate();
        var compType = CompType.FUNCTION;
        var liseners = react.useMemo(function () { return [
            {
                forceUpdate: forceUpdate,
                self: self,
                watchedProps: new Set(),
                role: Role.COMPONENT,
                compType: compType
            }
        ]; }, [identification]);
        react.useEffect(function () { return injector$1.subscribe(InjectedStoreClass, liseners, compType, identification); }, [
            identification
        ]);
        return react.useMemo(function () { return injector$1.get(InjectedStoreClass, liseners, identification); }, [identification]);
    };

    var injector$2 = getInjector();
    var injection = function (InjectedStoreClass, identification) { return function (target, property) {
        var propertySymbol = Symbol(property);
        var clazz = InjectedStoreClass;
        if (target instanceof clazz) {
            logError("injection decorator can't be use to self!");
        }
        var liseners = [];
        return {
            enumerable: true,
            configurable: true,
            get: function () {
                var _this = this;
                var _a;
                if (!this[propertySymbol]) {
                    var forceUpdate = (_a = this.forceUpdate) === null || _a === void 0 ? void 0 : _a.bind(this);
                    var role = this instanceof StoreBase ? Role.STORE : Role.COMPONENT;
                    var compType = CompType.CLASS;
                    liseners = [
                        {
                            forceUpdate: forceUpdate,
                            self: this,
                            watchedProps: new Set(),
                            role: role,
                            compType: compType
                        }
                    ];
                    var componentWillUnmount_1 = this.componentWillUnmount;
                    var unsubscribe_1 = injector$2.subscribe(clazz, liseners, compType, identification);
                    this.componentWillUnmount = function () {
                        var args = [];
                        for (var _i = 0; _i < arguments.length; _i++) {
                            args[_i] = arguments[_i];
                        }
                        unsubscribe_1 === null || unsubscribe_1 === void 0 ? void 0 : unsubscribe_1();
                        componentWillUnmount_1 === null || componentWillUnmount_1 === void 0 ? void 0 : componentWillUnmount_1.call.apply(componentWillUnmount_1, __spread([_this], args));
                    };
                    this[propertySymbol] = injector$2.get(clazz, liseners, identification);
                }
                return this[propertySymbol];
            },
            set: function () {
                logError('store is readonly.');
            }
        };
    }; };

    var commonjsGlobal = typeof globalThis !== 'undefined' ? globalThis : typeof window !== 'undefined' ? window : typeof global !== 'undefined' ? global : typeof self !== 'undefined' ? self : {};

    /*! *****************************************************************************
    Copyright (C) Microsoft. All rights reserved.
    Licensed under the Apache License, Version 2.0 (the "License"); you may not use
    this file except in compliance with the License. You may obtain a copy of the
    License at http://www.apache.org/licenses/LICENSE-2.0

    THIS CODE IS PROVIDED ON AN *AS IS* BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
    KIND, EITHER EXPRESS OR IMPLIED, INCLUDING WITHOUT LIMITATION ANY IMPLIED
    WARRANTIES OR CONDITIONS OF TITLE, FITNESS FOR A PARTICULAR PURPOSE,
    MERCHANTABLITY OR NON-INFRINGEMENT.

    See the Apache Version 2.0 License for specific language governing permissions
    and limitations under the License.
    ***************************************************************************** */
    var Reflect$1;
    (function (Reflect) {
        // Metadata Proposal
        // https://rbuckton.github.io/reflect-metadata/
        (function (factory) {
            var root = typeof commonjsGlobal === "object" ? commonjsGlobal :
                typeof self === "object" ? self :
                    typeof this === "object" ? this :
                        Function("return this;")();
            var exporter = makeExporter(Reflect);
            if (typeof root.Reflect === "undefined") {
                root.Reflect = Reflect;
            }
            else {
                exporter = makeExporter(root.Reflect, exporter);
            }
            factory(exporter);
            function makeExporter(target, previous) {
                return function (key, value) {
                    if (typeof target[key] !== "function") {
                        Object.defineProperty(target, key, { configurable: true, writable: true, value: value });
                    }
                    if (previous)
                        previous(key, value);
                };
            }
        })(function (exporter) {
            var hasOwn = Object.prototype.hasOwnProperty;
            // feature test for Symbol support
            var supportsSymbol = typeof Symbol === "function";
            var toPrimitiveSymbol = supportsSymbol && typeof Symbol.toPrimitive !== "undefined" ? Symbol.toPrimitive : "@@toPrimitive";
            var iteratorSymbol = supportsSymbol && typeof Symbol.iterator !== "undefined" ? Symbol.iterator : "@@iterator";
            var supportsCreate = typeof Object.create === "function"; // feature test for Object.create support
            var supportsProto = { __proto__: [] } instanceof Array; // feature test for __proto__ support
            var downLevel = !supportsCreate && !supportsProto;
            var HashMap = {
                // create an object in dictionary mode (a.k.a. "slow" mode in v8)
                create: supportsCreate
                    ? function () { return MakeDictionary(Object.create(null)); }
                    : supportsProto
                        ? function () { return MakeDictionary({ __proto__: null }); }
                        : function () { return MakeDictionary({}); },
                has: downLevel
                    ? function (map, key) { return hasOwn.call(map, key); }
                    : function (map, key) { return key in map; },
                get: downLevel
                    ? function (map, key) { return hasOwn.call(map, key) ? map[key] : undefined; }
                    : function (map, key) { return map[key]; },
            };
            // Load global or shim versions of Map, Set, and WeakMap
            var functionPrototype = Object.getPrototypeOf(Function);
            var usePolyfill = typeof process === "object" && process.env && process.env["REFLECT_METADATA_USE_MAP_POLYFILL"] === "true";
            var _Map = !usePolyfill && typeof Map === "function" && typeof Map.prototype.entries === "function" ? Map : CreateMapPolyfill();
            var _Set = !usePolyfill && typeof Set === "function" && typeof Set.prototype.entries === "function" ? Set : CreateSetPolyfill();
            var _WeakMap = !usePolyfill && typeof WeakMap === "function" ? WeakMap : CreateWeakMapPolyfill();
            // [[Metadata]] internal slot
            // https://rbuckton.github.io/reflect-metadata/#ordinary-object-internal-methods-and-internal-slots
            var Metadata = new _WeakMap();
            /**
             * Applies a set of decorators to a property of a target object.
             * @param decorators An array of decorators.
             * @param target The target object.
             * @param propertyKey (Optional) The property key to decorate.
             * @param attributes (Optional) The property descriptor for the target key.
             * @remarks Decorators are applied in reverse order.
             * @example
             *
             *     class Example {
             *         // property declarations are not part of ES6, though they are valid in TypeScript:
             *         // static staticProperty;
             *         // property;
             *
             *         constructor(p) { }
             *         static staticMethod(p) { }
             *         method(p) { }
             *     }
             *
             *     // constructor
             *     Example = Reflect.decorate(decoratorsArray, Example);
             *
             *     // property (on constructor)
             *     Reflect.decorate(decoratorsArray, Example, "staticProperty");
             *
             *     // property (on prototype)
             *     Reflect.decorate(decoratorsArray, Example.prototype, "property");
             *
             *     // method (on constructor)
             *     Object.defineProperty(Example, "staticMethod",
             *         Reflect.decorate(decoratorsArray, Example, "staticMethod",
             *             Object.getOwnPropertyDescriptor(Example, "staticMethod")));
             *
             *     // method (on prototype)
             *     Object.defineProperty(Example.prototype, "method",
             *         Reflect.decorate(decoratorsArray, Example.prototype, "method",
             *             Object.getOwnPropertyDescriptor(Example.prototype, "method")));
             *
             */
            function decorate(decorators, target, propertyKey, attributes) {
                if (!IsUndefined(propertyKey)) {
                    if (!IsArray(decorators))
                        throw new TypeError();
                    if (!IsObject(target))
                        throw new TypeError();
                    if (!IsObject(attributes) && !IsUndefined(attributes) && !IsNull(attributes))
                        throw new TypeError();
                    if (IsNull(attributes))
                        attributes = undefined;
                    propertyKey = ToPropertyKey(propertyKey);
                    return DecorateProperty(decorators, target, propertyKey, attributes);
                }
                else {
                    if (!IsArray(decorators))
                        throw new TypeError();
                    if (!IsConstructor(target))
                        throw new TypeError();
                    return DecorateConstructor(decorators, target);
                }
            }
            exporter("decorate", decorate);
            // 4.1.2 Reflect.metadata(metadataKey, metadataValue)
            // https://rbuckton.github.io/reflect-metadata/#reflect.metadata
            /**
             * A default metadata decorator factory that can be used on a class, class member, or parameter.
             * @param metadataKey The key for the metadata entry.
             * @param metadataValue The value for the metadata entry.
             * @returns A decorator function.
             * @remarks
             * If `metadataKey` is already defined for the target and target key, the
             * metadataValue for that key will be overwritten.
             * @example
             *
             *     // constructor
             *     @Reflect.metadata(key, value)
             *     class Example {
             *     }
             *
             *     // property (on constructor, TypeScript only)
             *     class Example {
             *         @Reflect.metadata(key, value)
             *         static staticProperty;
             *     }
             *
             *     // property (on prototype, TypeScript only)
             *     class Example {
             *         @Reflect.metadata(key, value)
             *         property;
             *     }
             *
             *     // method (on constructor)
             *     class Example {
             *         @Reflect.metadata(key, value)
             *         static staticMethod() { }
             *     }
             *
             *     // method (on prototype)
             *     class Example {
             *         @Reflect.metadata(key, value)
             *         method() { }
             *     }
             *
             */
            function metadata(metadataKey, metadataValue) {
                function decorator(target, propertyKey) {
                    if (!IsObject(target))
                        throw new TypeError();
                    if (!IsUndefined(propertyKey) && !IsPropertyKey(propertyKey))
                        throw new TypeError();
                    OrdinaryDefineOwnMetadata(metadataKey, metadataValue, target, propertyKey);
                }
                return decorator;
            }
            exporter("metadata", metadata);
            /**
             * Define a unique metadata entry on the target.
             * @param metadataKey A key used to store and retrieve metadata.
             * @param metadataValue A value that contains attached metadata.
             * @param target The target object on which to define metadata.
             * @param propertyKey (Optional) The property key for the target.
             * @example
             *
             *     class Example {
             *         // property declarations are not part of ES6, though they are valid in TypeScript:
             *         // static staticProperty;
             *         // property;
             *
             *         constructor(p) { }
             *         static staticMethod(p) { }
             *         method(p) { }
             *     }
             *
             *     // constructor
             *     Reflect.defineMetadata("custom:annotation", options, Example);
             *
             *     // property (on constructor)
             *     Reflect.defineMetadata("custom:annotation", options, Example, "staticProperty");
             *
             *     // property (on prototype)
             *     Reflect.defineMetadata("custom:annotation", options, Example.prototype, "property");
             *
             *     // method (on constructor)
             *     Reflect.defineMetadata("custom:annotation", options, Example, "staticMethod");
             *
             *     // method (on prototype)
             *     Reflect.defineMetadata("custom:annotation", options, Example.prototype, "method");
             *
             *     // decorator factory as metadata-producing annotation.
             *     function MyAnnotation(options): Decorator {
             *         return (target, key?) => Reflect.defineMetadata("custom:annotation", options, target, key);
             *     }
             *
             */
            function defineMetadata(metadataKey, metadataValue, target, propertyKey) {
                if (!IsObject(target))
                    throw new TypeError();
                if (!IsUndefined(propertyKey))
                    propertyKey = ToPropertyKey(propertyKey);
                return OrdinaryDefineOwnMetadata(metadataKey, metadataValue, target, propertyKey);
            }
            exporter("defineMetadata", defineMetadata);
            /**
             * Gets a value indicating whether the target object or its prototype chain has the provided metadata key defined.
             * @param metadataKey A key used to store and retrieve metadata.
             * @param target The target object on which the metadata is defined.
             * @param propertyKey (Optional) The property key for the target.
             * @returns `true` if the metadata key was defined on the target object or its prototype chain; otherwise, `false`.
             * @example
             *
             *     class Example {
             *         // property declarations are not part of ES6, though they are valid in TypeScript:
             *         // static staticProperty;
             *         // property;
             *
             *         constructor(p) { }
             *         static staticMethod(p) { }
             *         method(p) { }
             *     }
             *
             *     // constructor
             *     result = Reflect.hasMetadata("custom:annotation", Example);
             *
             *     // property (on constructor)
             *     result = Reflect.hasMetadata("custom:annotation", Example, "staticProperty");
             *
             *     // property (on prototype)
             *     result = Reflect.hasMetadata("custom:annotation", Example.prototype, "property");
             *
             *     // method (on constructor)
             *     result = Reflect.hasMetadata("custom:annotation", Example, "staticMethod");
             *
             *     // method (on prototype)
             *     result = Reflect.hasMetadata("custom:annotation", Example.prototype, "method");
             *
             */
            function hasMetadata(metadataKey, target, propertyKey) {
                if (!IsObject(target))
                    throw new TypeError();
                if (!IsUndefined(propertyKey))
                    propertyKey = ToPropertyKey(propertyKey);
                return OrdinaryHasMetadata(metadataKey, target, propertyKey);
            }
            exporter("hasMetadata", hasMetadata);
            /**
             * Gets a value indicating whether the target object has the provided metadata key defined.
             * @param metadataKey A key used to store and retrieve metadata.
             * @param target The target object on which the metadata is defined.
             * @param propertyKey (Optional) The property key for the target.
             * @returns `true` if the metadata key was defined on the target object; otherwise, `false`.
             * @example
             *
             *     class Example {
             *         // property declarations are not part of ES6, though they are valid in TypeScript:
             *         // static staticProperty;
             *         // property;
             *
             *         constructor(p) { }
             *         static staticMethod(p) { }
             *         method(p) { }
             *     }
             *
             *     // constructor
             *     result = Reflect.hasOwnMetadata("custom:annotation", Example);
             *
             *     // property (on constructor)
             *     result = Reflect.hasOwnMetadata("custom:annotation", Example, "staticProperty");
             *
             *     // property (on prototype)
             *     result = Reflect.hasOwnMetadata("custom:annotation", Example.prototype, "property");
             *
             *     // method (on constructor)
             *     result = Reflect.hasOwnMetadata("custom:annotation", Example, "staticMethod");
             *
             *     // method (on prototype)
             *     result = Reflect.hasOwnMetadata("custom:annotation", Example.prototype, "method");
             *
             */
            function hasOwnMetadata(metadataKey, target, propertyKey) {
                if (!IsObject(target))
                    throw new TypeError();
                if (!IsUndefined(propertyKey))
                    propertyKey = ToPropertyKey(propertyKey);
                return OrdinaryHasOwnMetadata(metadataKey, target, propertyKey);
            }
            exporter("hasOwnMetadata", hasOwnMetadata);
            /**
             * Gets the metadata value for the provided metadata key on the target object or its prototype chain.
             * @param metadataKey A key used to store and retrieve metadata.
             * @param target The target object on which the metadata is defined.
             * @param propertyKey (Optional) The property key for the target.
             * @returns The metadata value for the metadata key if found; otherwise, `undefined`.
             * @example
             *
             *     class Example {
             *         // property declarations are not part of ES6, though they are valid in TypeScript:
             *         // static staticProperty;
             *         // property;
             *
             *         constructor(p) { }
             *         static staticMethod(p) { }
             *         method(p) { }
             *     }
             *
             *     // constructor
             *     result = Reflect.getMetadata("custom:annotation", Example);
             *
             *     // property (on constructor)
             *     result = Reflect.getMetadata("custom:annotation", Example, "staticProperty");
             *
             *     // property (on prototype)
             *     result = Reflect.getMetadata("custom:annotation", Example.prototype, "property");
             *
             *     // method (on constructor)
             *     result = Reflect.getMetadata("custom:annotation", Example, "staticMethod");
             *
             *     // method (on prototype)
             *     result = Reflect.getMetadata("custom:annotation", Example.prototype, "method");
             *
             */
            function getMetadata(metadataKey, target, propertyKey) {
                if (!IsObject(target))
                    throw new TypeError();
                if (!IsUndefined(propertyKey))
                    propertyKey = ToPropertyKey(propertyKey);
                return OrdinaryGetMetadata(metadataKey, target, propertyKey);
            }
            exporter("getMetadata", getMetadata);
            /**
             * Gets the metadata value for the provided metadata key on the target object.
             * @param metadataKey A key used to store and retrieve metadata.
             * @param target The target object on which the metadata is defined.
             * @param propertyKey (Optional) The property key for the target.
             * @returns The metadata value for the metadata key if found; otherwise, `undefined`.
             * @example
             *
             *     class Example {
             *         // property declarations are not part of ES6, though they are valid in TypeScript:
             *         // static staticProperty;
             *         // property;
             *
             *         constructor(p) { }
             *         static staticMethod(p) { }
             *         method(p) { }
             *     }
             *
             *     // constructor
             *     result = Reflect.getOwnMetadata("custom:annotation", Example);
             *
             *     // property (on constructor)
             *     result = Reflect.getOwnMetadata("custom:annotation", Example, "staticProperty");
             *
             *     // property (on prototype)
             *     result = Reflect.getOwnMetadata("custom:annotation", Example.prototype, "property");
             *
             *     // method (on constructor)
             *     result = Reflect.getOwnMetadata("custom:annotation", Example, "staticMethod");
             *
             *     // method (on prototype)
             *     result = Reflect.getOwnMetadata("custom:annotation", Example.prototype, "method");
             *
             */
            function getOwnMetadata(metadataKey, target, propertyKey) {
                if (!IsObject(target))
                    throw new TypeError();
                if (!IsUndefined(propertyKey))
                    propertyKey = ToPropertyKey(propertyKey);
                return OrdinaryGetOwnMetadata(metadataKey, target, propertyKey);
            }
            exporter("getOwnMetadata", getOwnMetadata);
            /**
             * Gets the metadata keys defined on the target object or its prototype chain.
             * @param target The target object on which the metadata is defined.
             * @param propertyKey (Optional) The property key for the target.
             * @returns An array of unique metadata keys.
             * @example
             *
             *     class Example {
             *         // property declarations are not part of ES6, though they are valid in TypeScript:
             *         // static staticProperty;
             *         // property;
             *
             *         constructor(p) { }
             *         static staticMethod(p) { }
             *         method(p) { }
             *     }
             *
             *     // constructor
             *     result = Reflect.getMetadataKeys(Example);
             *
             *     // property (on constructor)
             *     result = Reflect.getMetadataKeys(Example, "staticProperty");
             *
             *     // property (on prototype)
             *     result = Reflect.getMetadataKeys(Example.prototype, "property");
             *
             *     // method (on constructor)
             *     result = Reflect.getMetadataKeys(Example, "staticMethod");
             *
             *     // method (on prototype)
             *     result = Reflect.getMetadataKeys(Example.prototype, "method");
             *
             */
            function getMetadataKeys(target, propertyKey) {
                if (!IsObject(target))
                    throw new TypeError();
                if (!IsUndefined(propertyKey))
                    propertyKey = ToPropertyKey(propertyKey);
                return OrdinaryMetadataKeys(target, propertyKey);
            }
            exporter("getMetadataKeys", getMetadataKeys);
            /**
             * Gets the unique metadata keys defined on the target object.
             * @param target The target object on which the metadata is defined.
             * @param propertyKey (Optional) The property key for the target.
             * @returns An array of unique metadata keys.
             * @example
             *
             *     class Example {
             *         // property declarations are not part of ES6, though they are valid in TypeScript:
             *         // static staticProperty;
             *         // property;
             *
             *         constructor(p) { }
             *         static staticMethod(p) { }
             *         method(p) { }
             *     }
             *
             *     // constructor
             *     result = Reflect.getOwnMetadataKeys(Example);
             *
             *     // property (on constructor)
             *     result = Reflect.getOwnMetadataKeys(Example, "staticProperty");
             *
             *     // property (on prototype)
             *     result = Reflect.getOwnMetadataKeys(Example.prototype, "property");
             *
             *     // method (on constructor)
             *     result = Reflect.getOwnMetadataKeys(Example, "staticMethod");
             *
             *     // method (on prototype)
             *     result = Reflect.getOwnMetadataKeys(Example.prototype, "method");
             *
             */
            function getOwnMetadataKeys(target, propertyKey) {
                if (!IsObject(target))
                    throw new TypeError();
                if (!IsUndefined(propertyKey))
                    propertyKey = ToPropertyKey(propertyKey);
                return OrdinaryOwnMetadataKeys(target, propertyKey);
            }
            exporter("getOwnMetadataKeys", getOwnMetadataKeys);
            /**
             * Deletes the metadata entry from the target object with the provided key.
             * @param metadataKey A key used to store and retrieve metadata.
             * @param target The target object on which the metadata is defined.
             * @param propertyKey (Optional) The property key for the target.
             * @returns `true` if the metadata entry was found and deleted; otherwise, false.
             * @example
             *
             *     class Example {
             *         // property declarations are not part of ES6, though they are valid in TypeScript:
             *         // static staticProperty;
             *         // property;
             *
             *         constructor(p) { }
             *         static staticMethod(p) { }
             *         method(p) { }
             *     }
             *
             *     // constructor
             *     result = Reflect.deleteMetadata("custom:annotation", Example);
             *
             *     // property (on constructor)
             *     result = Reflect.deleteMetadata("custom:annotation", Example, "staticProperty");
             *
             *     // property (on prototype)
             *     result = Reflect.deleteMetadata("custom:annotation", Example.prototype, "property");
             *
             *     // method (on constructor)
             *     result = Reflect.deleteMetadata("custom:annotation", Example, "staticMethod");
             *
             *     // method (on prototype)
             *     result = Reflect.deleteMetadata("custom:annotation", Example.prototype, "method");
             *
             */
            function deleteMetadata(metadataKey, target, propertyKey) {
                if (!IsObject(target))
                    throw new TypeError();
                if (!IsUndefined(propertyKey))
                    propertyKey = ToPropertyKey(propertyKey);
                var metadataMap = GetOrCreateMetadataMap(target, propertyKey, /*Create*/ false);
                if (IsUndefined(metadataMap))
                    return false;
                if (!metadataMap.delete(metadataKey))
                    return false;
                if (metadataMap.size > 0)
                    return true;
                var targetMetadata = Metadata.get(target);
                targetMetadata.delete(propertyKey);
                if (targetMetadata.size > 0)
                    return true;
                Metadata.delete(target);
                return true;
            }
            exporter("deleteMetadata", deleteMetadata);
            function DecorateConstructor(decorators, target) {
                for (var i = decorators.length - 1; i >= 0; --i) {
                    var decorator = decorators[i];
                    var decorated = decorator(target);
                    if (!IsUndefined(decorated) && !IsNull(decorated)) {
                        if (!IsConstructor(decorated))
                            throw new TypeError();
                        target = decorated;
                    }
                }
                return target;
            }
            function DecorateProperty(decorators, target, propertyKey, descriptor) {
                for (var i = decorators.length - 1; i >= 0; --i) {
                    var decorator = decorators[i];
                    var decorated = decorator(target, propertyKey, descriptor);
                    if (!IsUndefined(decorated) && !IsNull(decorated)) {
                        if (!IsObject(decorated))
                            throw new TypeError();
                        descriptor = decorated;
                    }
                }
                return descriptor;
            }
            function GetOrCreateMetadataMap(O, P, Create) {
                var targetMetadata = Metadata.get(O);
                if (IsUndefined(targetMetadata)) {
                    if (!Create)
                        return undefined;
                    targetMetadata = new _Map();
                    Metadata.set(O, targetMetadata);
                }
                var metadataMap = targetMetadata.get(P);
                if (IsUndefined(metadataMap)) {
                    if (!Create)
                        return undefined;
                    metadataMap = new _Map();
                    targetMetadata.set(P, metadataMap);
                }
                return metadataMap;
            }
            // 3.1.1.1 OrdinaryHasMetadata(MetadataKey, O, P)
            // https://rbuckton.github.io/reflect-metadata/#ordinaryhasmetadata
            function OrdinaryHasMetadata(MetadataKey, O, P) {
                var hasOwn = OrdinaryHasOwnMetadata(MetadataKey, O, P);
                if (hasOwn)
                    return true;
                var parent = OrdinaryGetPrototypeOf(O);
                if (!IsNull(parent))
                    return OrdinaryHasMetadata(MetadataKey, parent, P);
                return false;
            }
            // 3.1.2.1 OrdinaryHasOwnMetadata(MetadataKey, O, P)
            // https://rbuckton.github.io/reflect-metadata/#ordinaryhasownmetadata
            function OrdinaryHasOwnMetadata(MetadataKey, O, P) {
                var metadataMap = GetOrCreateMetadataMap(O, P, /*Create*/ false);
                if (IsUndefined(metadataMap))
                    return false;
                return ToBoolean(metadataMap.has(MetadataKey));
            }
            // 3.1.3.1 OrdinaryGetMetadata(MetadataKey, O, P)
            // https://rbuckton.github.io/reflect-metadata/#ordinarygetmetadata
            function OrdinaryGetMetadata(MetadataKey, O, P) {
                var hasOwn = OrdinaryHasOwnMetadata(MetadataKey, O, P);
                if (hasOwn)
                    return OrdinaryGetOwnMetadata(MetadataKey, O, P);
                var parent = OrdinaryGetPrototypeOf(O);
                if (!IsNull(parent))
                    return OrdinaryGetMetadata(MetadataKey, parent, P);
                return undefined;
            }
            // 3.1.4.1 OrdinaryGetOwnMetadata(MetadataKey, O, P)
            // https://rbuckton.github.io/reflect-metadata/#ordinarygetownmetadata
            function OrdinaryGetOwnMetadata(MetadataKey, O, P) {
                var metadataMap = GetOrCreateMetadataMap(O, P, /*Create*/ false);
                if (IsUndefined(metadataMap))
                    return undefined;
                return metadataMap.get(MetadataKey);
            }
            // 3.1.5.1 OrdinaryDefineOwnMetadata(MetadataKey, MetadataValue, O, P)
            // https://rbuckton.github.io/reflect-metadata/#ordinarydefineownmetadata
            function OrdinaryDefineOwnMetadata(MetadataKey, MetadataValue, O, P) {
                var metadataMap = GetOrCreateMetadataMap(O, P, /*Create*/ true);
                metadataMap.set(MetadataKey, MetadataValue);
            }
            // 3.1.6.1 OrdinaryMetadataKeys(O, P)
            // https://rbuckton.github.io/reflect-metadata/#ordinarymetadatakeys
            function OrdinaryMetadataKeys(O, P) {
                var ownKeys = OrdinaryOwnMetadataKeys(O, P);
                var parent = OrdinaryGetPrototypeOf(O);
                if (parent === null)
                    return ownKeys;
                var parentKeys = OrdinaryMetadataKeys(parent, P);
                if (parentKeys.length <= 0)
                    return ownKeys;
                if (ownKeys.length <= 0)
                    return parentKeys;
                var set = new _Set();
                var keys = [];
                for (var _i = 0, ownKeys_1 = ownKeys; _i < ownKeys_1.length; _i++) {
                    var key = ownKeys_1[_i];
                    var hasKey = set.has(key);
                    if (!hasKey) {
                        set.add(key);
                        keys.push(key);
                    }
                }
                for (var _a = 0, parentKeys_1 = parentKeys; _a < parentKeys_1.length; _a++) {
                    var key = parentKeys_1[_a];
                    var hasKey = set.has(key);
                    if (!hasKey) {
                        set.add(key);
                        keys.push(key);
                    }
                }
                return keys;
            }
            // 3.1.7.1 OrdinaryOwnMetadataKeys(O, P)
            // https://rbuckton.github.io/reflect-metadata/#ordinaryownmetadatakeys
            function OrdinaryOwnMetadataKeys(O, P) {
                var keys = [];
                var metadataMap = GetOrCreateMetadataMap(O, P, /*Create*/ false);
                if (IsUndefined(metadataMap))
                    return keys;
                var keysObj = metadataMap.keys();
                var iterator = GetIterator(keysObj);
                var k = 0;
                while (true) {
                    var next = IteratorStep(iterator);
                    if (!next) {
                        keys.length = k;
                        return keys;
                    }
                    var nextValue = IteratorValue(next);
                    try {
                        keys[k] = nextValue;
                    }
                    catch (e) {
                        try {
                            IteratorClose(iterator);
                        }
                        finally {
                            throw e;
                        }
                    }
                    k++;
                }
            }
            // 6 ECMAScript Data Typ0es and Values
            // https://tc39.github.io/ecma262/#sec-ecmascript-data-types-and-values
            function Type(x) {
                if (x === null)
                    return 1 /* Null */;
                switch (typeof x) {
                    case "undefined": return 0 /* Undefined */;
                    case "boolean": return 2 /* Boolean */;
                    case "string": return 3 /* String */;
                    case "symbol": return 4 /* Symbol */;
                    case "number": return 5 /* Number */;
                    case "object": return x === null ? 1 /* Null */ : 6 /* Object */;
                    default: return 6 /* Object */;
                }
            }
            // 6.1.1 The Undefined Type
            // https://tc39.github.io/ecma262/#sec-ecmascript-language-types-undefined-type
            function IsUndefined(x) {
                return x === undefined;
            }
            // 6.1.2 The Null Type
            // https://tc39.github.io/ecma262/#sec-ecmascript-language-types-null-type
            function IsNull(x) {
                return x === null;
            }
            // 6.1.5 The Symbol Type
            // https://tc39.github.io/ecma262/#sec-ecmascript-language-types-symbol-type
            function IsSymbol(x) {
                return typeof x === "symbol";
            }
            // 6.1.7 The Object Type
            // https://tc39.github.io/ecma262/#sec-object-type
            function IsObject(x) {
                return typeof x === "object" ? x !== null : typeof x === "function";
            }
            // 7.1 Type Conversion
            // https://tc39.github.io/ecma262/#sec-type-conversion
            // 7.1.1 ToPrimitive(input [, PreferredType])
            // https://tc39.github.io/ecma262/#sec-toprimitive
            function ToPrimitive(input, PreferredType) {
                switch (Type(input)) {
                    case 0 /* Undefined */: return input;
                    case 1 /* Null */: return input;
                    case 2 /* Boolean */: return input;
                    case 3 /* String */: return input;
                    case 4 /* Symbol */: return input;
                    case 5 /* Number */: return input;
                }
                var hint = PreferredType === 3 /* String */ ? "string" : PreferredType === 5 /* Number */ ? "number" : "default";
                var exoticToPrim = GetMethod(input, toPrimitiveSymbol);
                if (exoticToPrim !== undefined) {
                    var result = exoticToPrim.call(input, hint);
                    if (IsObject(result))
                        throw new TypeError();
                    return result;
                }
                return OrdinaryToPrimitive(input, hint === "default" ? "number" : hint);
            }
            // 7.1.1.1 OrdinaryToPrimitive(O, hint)
            // https://tc39.github.io/ecma262/#sec-ordinarytoprimitive
            function OrdinaryToPrimitive(O, hint) {
                if (hint === "string") {
                    var toString_1 = O.toString;
                    if (IsCallable(toString_1)) {
                        var result = toString_1.call(O);
                        if (!IsObject(result))
                            return result;
                    }
                    var valueOf = O.valueOf;
                    if (IsCallable(valueOf)) {
                        var result = valueOf.call(O);
                        if (!IsObject(result))
                            return result;
                    }
                }
                else {
                    var valueOf = O.valueOf;
                    if (IsCallable(valueOf)) {
                        var result = valueOf.call(O);
                        if (!IsObject(result))
                            return result;
                    }
                    var toString_2 = O.toString;
                    if (IsCallable(toString_2)) {
                        var result = toString_2.call(O);
                        if (!IsObject(result))
                            return result;
                    }
                }
                throw new TypeError();
            }
            // 7.1.2 ToBoolean(argument)
            // https://tc39.github.io/ecma262/2016/#sec-toboolean
            function ToBoolean(argument) {
                return !!argument;
            }
            // 7.1.12 ToString(argument)
            // https://tc39.github.io/ecma262/#sec-tostring
            function ToString(argument) {
                return "" + argument;
            }
            // 7.1.14 ToPropertyKey(argument)
            // https://tc39.github.io/ecma262/#sec-topropertykey
            function ToPropertyKey(argument) {
                var key = ToPrimitive(argument, 3 /* String */);
                if (IsSymbol(key))
                    return key;
                return ToString(key);
            }
            // 7.2 Testing and Comparison Operations
            // https://tc39.github.io/ecma262/#sec-testing-and-comparison-operations
            // 7.2.2 IsArray(argument)
            // https://tc39.github.io/ecma262/#sec-isarray
            function IsArray(argument) {
                return Array.isArray
                    ? Array.isArray(argument)
                    : argument instanceof Object
                        ? argument instanceof Array
                        : Object.prototype.toString.call(argument) === "[object Array]";
            }
            // 7.2.3 IsCallable(argument)
            // https://tc39.github.io/ecma262/#sec-iscallable
            function IsCallable(argument) {
                // NOTE: This is an approximation as we cannot check for [[Call]] internal method.
                return typeof argument === "function";
            }
            // 7.2.4 IsConstructor(argument)
            // https://tc39.github.io/ecma262/#sec-isconstructor
            function IsConstructor(argument) {
                // NOTE: This is an approximation as we cannot check for [[Construct]] internal method.
                return typeof argument === "function";
            }
            // 7.2.7 IsPropertyKey(argument)
            // https://tc39.github.io/ecma262/#sec-ispropertykey
            function IsPropertyKey(argument) {
                switch (Type(argument)) {
                    case 3 /* String */: return true;
                    case 4 /* Symbol */: return true;
                    default: return false;
                }
            }
            // 7.3 Operations on Objects
            // https://tc39.github.io/ecma262/#sec-operations-on-objects
            // 7.3.9 GetMethod(V, P)
            // https://tc39.github.io/ecma262/#sec-getmethod
            function GetMethod(V, P) {
                var func = V[P];
                if (func === undefined || func === null)
                    return undefined;
                if (!IsCallable(func))
                    throw new TypeError();
                return func;
            }
            // 7.4 Operations on Iterator Objects
            // https://tc39.github.io/ecma262/#sec-operations-on-iterator-objects
            function GetIterator(obj) {
                var method = GetMethod(obj, iteratorSymbol);
                if (!IsCallable(method))
                    throw new TypeError(); // from Call
                var iterator = method.call(obj);
                if (!IsObject(iterator))
                    throw new TypeError();
                return iterator;
            }
            // 7.4.4 IteratorValue(iterResult)
            // https://tc39.github.io/ecma262/2016/#sec-iteratorvalue
            function IteratorValue(iterResult) {
                return iterResult.value;
            }
            // 7.4.5 IteratorStep(iterator)
            // https://tc39.github.io/ecma262/#sec-iteratorstep
            function IteratorStep(iterator) {
                var result = iterator.next();
                return result.done ? false : result;
            }
            // 7.4.6 IteratorClose(iterator, completion)
            // https://tc39.github.io/ecma262/#sec-iteratorclose
            function IteratorClose(iterator) {
                var f = iterator["return"];
                if (f)
                    f.call(iterator);
            }
            // 9.1 Ordinary Object Internal Methods and Internal Slots
            // https://tc39.github.io/ecma262/#sec-ordinary-object-internal-methods-and-internal-slots
            // 9.1.1.1 OrdinaryGetPrototypeOf(O)
            // https://tc39.github.io/ecma262/#sec-ordinarygetprototypeof
            function OrdinaryGetPrototypeOf(O) {
                var proto = Object.getPrototypeOf(O);
                if (typeof O !== "function" || O === functionPrototype)
                    return proto;
                // TypeScript doesn't set __proto__ in ES5, as it's non-standard.
                // Try to determine the superclass constructor. Compatible implementations
                // must either set __proto__ on a subclass constructor to the superclass constructor,
                // or ensure each class has a valid `constructor` property on its prototype that
                // points back to the constructor.
                // If this is not the same as Function.[[Prototype]], then this is definately inherited.
                // This is the case when in ES6 or when using __proto__ in a compatible browser.
                if (proto !== functionPrototype)
                    return proto;
                // If the super prototype is Object.prototype, null, or undefined, then we cannot determine the heritage.
                var prototype = O.prototype;
                var prototypeProto = prototype && Object.getPrototypeOf(prototype);
                if (prototypeProto == null || prototypeProto === Object.prototype)
                    return proto;
                // If the constructor was not a function, then we cannot determine the heritage.
                var constructor = prototypeProto.constructor;
                if (typeof constructor !== "function")
                    return proto;
                // If we have some kind of self-reference, then we cannot determine the heritage.
                if (constructor === O)
                    return proto;
                // we have a pretty good guess at the heritage.
                return constructor;
            }
            // naive Map shim
            function CreateMapPolyfill() {
                var cacheSentinel = {};
                var arraySentinel = [];
                var MapIterator = /** @class */ (function () {
                    function MapIterator(keys, values, selector) {
                        this._index = 0;
                        this._keys = keys;
                        this._values = values;
                        this._selector = selector;
                    }
                    MapIterator.prototype["@@iterator"] = function () { return this; };
                    MapIterator.prototype[iteratorSymbol] = function () { return this; };
                    MapIterator.prototype.next = function () {
                        var index = this._index;
                        if (index >= 0 && index < this._keys.length) {
                            var result = this._selector(this._keys[index], this._values[index]);
                            if (index + 1 >= this._keys.length) {
                                this._index = -1;
                                this._keys = arraySentinel;
                                this._values = arraySentinel;
                            }
                            else {
                                this._index++;
                            }
                            return { value: result, done: false };
                        }
                        return { value: undefined, done: true };
                    };
                    MapIterator.prototype.throw = function (error) {
                        if (this._index >= 0) {
                            this._index = -1;
                            this._keys = arraySentinel;
                            this._values = arraySentinel;
                        }
                        throw error;
                    };
                    MapIterator.prototype.return = function (value) {
                        if (this._index >= 0) {
                            this._index = -1;
                            this._keys = arraySentinel;
                            this._values = arraySentinel;
                        }
                        return { value: value, done: true };
                    };
                    return MapIterator;
                }());
                return /** @class */ (function () {
                    function Map() {
                        this._keys = [];
                        this._values = [];
                        this._cacheKey = cacheSentinel;
                        this._cacheIndex = -2;
                    }
                    Object.defineProperty(Map.prototype, "size", {
                        get: function () { return this._keys.length; },
                        enumerable: true,
                        configurable: true
                    });
                    Map.prototype.has = function (key) { return this._find(key, /*insert*/ false) >= 0; };
                    Map.prototype.get = function (key) {
                        var index = this._find(key, /*insert*/ false);
                        return index >= 0 ? this._values[index] : undefined;
                    };
                    Map.prototype.set = function (key, value) {
                        var index = this._find(key, /*insert*/ true);
                        this._values[index] = value;
                        return this;
                    };
                    Map.prototype.delete = function (key) {
                        var index = this._find(key, /*insert*/ false);
                        if (index >= 0) {
                            var size = this._keys.length;
                            for (var i = index + 1; i < size; i++) {
                                this._keys[i - 1] = this._keys[i];
                                this._values[i - 1] = this._values[i];
                            }
                            this._keys.length--;
                            this._values.length--;
                            if (key === this._cacheKey) {
                                this._cacheKey = cacheSentinel;
                                this._cacheIndex = -2;
                            }
                            return true;
                        }
                        return false;
                    };
                    Map.prototype.clear = function () {
                        this._keys.length = 0;
                        this._values.length = 0;
                        this._cacheKey = cacheSentinel;
                        this._cacheIndex = -2;
                    };
                    Map.prototype.keys = function () { return new MapIterator(this._keys, this._values, getKey); };
                    Map.prototype.values = function () { return new MapIterator(this._keys, this._values, getValue); };
                    Map.prototype.entries = function () { return new MapIterator(this._keys, this._values, getEntry); };
                    Map.prototype["@@iterator"] = function () { return this.entries(); };
                    Map.prototype[iteratorSymbol] = function () { return this.entries(); };
                    Map.prototype._find = function (key, insert) {
                        if (this._cacheKey !== key) {
                            this._cacheIndex = this._keys.indexOf(this._cacheKey = key);
                        }
                        if (this._cacheIndex < 0 && insert) {
                            this._cacheIndex = this._keys.length;
                            this._keys.push(key);
                            this._values.push(undefined);
                        }
                        return this._cacheIndex;
                    };
                    return Map;
                }());
                function getKey(key, _) {
                    return key;
                }
                function getValue(_, value) {
                    return value;
                }
                function getEntry(key, value) {
                    return [key, value];
                }
            }
            // naive Set shim
            function CreateSetPolyfill() {
                return /** @class */ (function () {
                    function Set() {
                        this._map = new _Map();
                    }
                    Object.defineProperty(Set.prototype, "size", {
                        get: function () { return this._map.size; },
                        enumerable: true,
                        configurable: true
                    });
                    Set.prototype.has = function (value) { return this._map.has(value); };
                    Set.prototype.add = function (value) { return this._map.set(value, value), this; };
                    Set.prototype.delete = function (value) { return this._map.delete(value); };
                    Set.prototype.clear = function () { this._map.clear(); };
                    Set.prototype.keys = function () { return this._map.keys(); };
                    Set.prototype.values = function () { return this._map.values(); };
                    Set.prototype.entries = function () { return this._map.entries(); };
                    Set.prototype["@@iterator"] = function () { return this.keys(); };
                    Set.prototype[iteratorSymbol] = function () { return this.keys(); };
                    return Set;
                }());
            }
            // naive WeakMap shim
            function CreateWeakMapPolyfill() {
                var UUID_SIZE = 16;
                var keys = HashMap.create();
                var rootKey = CreateUniqueKey();
                return /** @class */ (function () {
                    function WeakMap() {
                        this._key = CreateUniqueKey();
                    }
                    WeakMap.prototype.has = function (target) {
                        var table = GetOrCreateWeakMapTable(target, /*create*/ false);
                        return table !== undefined ? HashMap.has(table, this._key) : false;
                    };
                    WeakMap.prototype.get = function (target) {
                        var table = GetOrCreateWeakMapTable(target, /*create*/ false);
                        return table !== undefined ? HashMap.get(table, this._key) : undefined;
                    };
                    WeakMap.prototype.set = function (target, value) {
                        var table = GetOrCreateWeakMapTable(target, /*create*/ true);
                        table[this._key] = value;
                        return this;
                    };
                    WeakMap.prototype.delete = function (target) {
                        var table = GetOrCreateWeakMapTable(target, /*create*/ false);
                        return table !== undefined ? delete table[this._key] : false;
                    };
                    WeakMap.prototype.clear = function () {
                        // NOTE: not a real clear, just makes the previous data unreachable
                        this._key = CreateUniqueKey();
                    };
                    return WeakMap;
                }());
                function CreateUniqueKey() {
                    var key;
                    do
                        key = "@@WeakMap@@" + CreateUUID();
                    while (HashMap.has(keys, key));
                    keys[key] = true;
                    return key;
                }
                function GetOrCreateWeakMapTable(target, create) {
                    if (!hasOwn.call(target, rootKey)) {
                        if (!create)
                            return undefined;
                        Object.defineProperty(target, rootKey, { value: HashMap.create() });
                    }
                    return target[rootKey];
                }
                function FillRandomBytes(buffer, size) {
                    for (var i = 0; i < size; ++i)
                        buffer[i] = Math.random() * 0xff | 0;
                    return buffer;
                }
                function GenRandomBytes(size) {
                    if (typeof Uint8Array === "function") {
                        if (typeof crypto !== "undefined")
                            return crypto.getRandomValues(new Uint8Array(size));
                        if (typeof msCrypto !== "undefined")
                            return msCrypto.getRandomValues(new Uint8Array(size));
                        return FillRandomBytes(new Uint8Array(size), size);
                    }
                    return FillRandomBytes(new Array(size), size);
                }
                function CreateUUID() {
                    var data = GenRandomBytes(UUID_SIZE);
                    // mark as random - RFC 4122 § 4.4
                    data[6] = data[6] & 0x4f | 0x40;
                    data[8] = data[8] & 0xbf | 0x80;
                    var result = "";
                    for (var offset = 0; offset < UUID_SIZE; ++offset) {
                        var byte = data[offset];
                        if (offset === 4 || offset === 6 || offset === 8)
                            result += "-";
                        if (byte < 16)
                            result += "0";
                        result += byte.toString(16).toLowerCase();
                    }
                    return result;
                }
            }
            // uses a heuristic used by v8 and chakra to force an object into dictionary mode.
            function MakeDictionary(obj) {
                obj.__ = undefined;
                delete obj.__;
                return obj;
            }
        });
    })(Reflect$1 || (Reflect$1 = {}));

    /**
     * config store
     *
     * @param  {Options<S>} options? store options
     *
     * @example
     * ```ts
     * import { store, StoreBase } from 'matrox'
     * `＠`store()
     * class Store extends StoreBase<Store> {
     * }
     *
     * ```
     *
     * @see https://github.com/anthhub/matrox#store
     */
    var store = function (options) { return function (target) {
        var ignoredProps = [];
        for (var key in target.prototype) {
            var ignoredProperty = Reflect.getMetadata('ignoredProperty', target.prototype, key);
            if (ignoredProperty) {
                ignoredProps.push(ignoredProperty);
            }
        }
        target[_meta] = { options: options, ignoredProps: ignoredProps };
        return target;
    }; };

    /**
     * create a store and return `useStore, injectStore, getStore, preloadStore, getState`
     *
     * @param  {Constructor<T>} InjectedStoreClass store class
     *
     * @example
     * ```ts
     * import { createStore} from 'matrox'
     *
     * const { useStore, injectStore, getStore, preloadStore, getState } = createStore(Store)
     * ```
     *
     * @see https://github.com/anthhub/matrox#createStore
     */
    var createStore = function (InjectedStoreClass, options) {
        if (options === void 0) { options = {}; }
        InjectedStoreClass = store(options)(InjectedStoreClass);
        /**
         * get a store for function component
         *
         * @param  identification? identification of reference store
         *
         * @example
         * const Comp: React.FC = () => {
         * const store = useStore()
         * return <div/>
         * }
         *
         * @see https://github.com/anthhub/matrox#useStore
         */
        var useStore = function (identification) {
            if (identification === void 0) { identification = ''; }
            return useInjection(InjectedStoreClass, identification);
        };
        /**
         * get a store for class component
         *
         * @param  identification? identification of reference store
         *
         * @example
         * class Comp extends React.Component{
         * `@`injectStore()
         * store: Readonly<Store>
         *  render(){
         *  return <div/>
         *  }
         * }
         *
         * @see https://github.com/anthhub/matrox#injectStore
         */
        var injectStore = function (identification) {
            if (identification === void 0) { identification = ''; }
            return injection(InjectedStoreClass, identification);
        };
        /**
         * get a store just for application store at outside of component, session store will return undefind
         *
         * @param  identification? identification of reference store
         *
         * @example
         * const store = getStore()
         *
         * @see https://github.com/anthhub/matrox#getStore
         */
        var getStore = function (identification) {
            if (identification === void 0) { identification = ''; }
            return getInjection(InjectedStoreClass, identification);
        };
        /**
         * preload a store just for application store
         *
         * @param  identification? identification of reference store
         *
         * @example
         * const store = getStore()
         *
         * @see https://github.com/anthhub/matrox#preloadStore
         */
        var preloadStore = function (identification) {
            if (identification === void 0) { identification = ''; }
            getStore(identification);
        };
        /**
         * get plain state object of store just for application one
         *
         * @param  identification? identification of reference store
         *
         * @example
         * const store = getStore()
         *
         * @see https://github.com/anthhub/matrox#getState
         */
        var getState = function (identification) {
            if (identification === void 0) { identification = ''; }
            var store$$1 = getStore(identification);
            if (!store$$1) {
                return {};
            }
            return store$$1.getState();
        };
        return { useStore: useStore, injectStore: injectStore, getStore: getStore, preloadStore: preloadStore, getState: getState };
    };

    /**
     * ignore property decorator ignore store fields to avoid trigger render when change it
     *
     * @example
     * ```ts
     * import { ignore} from 'matrox'
     * class Store extends StoreBase<Store> {
     * `@`ignore name = 'Jack'
     * }
     *
     * ```
     *
     * @see https://github.com/anthhub/matrox#ignore
     */
    var ignore = function (target, key) {
        Reflect.defineMetadata('ignoredProperty', key, target, key);
        var _val = target[key];
        if (delete target[key]) {
            // 创建新属性及其读取访问器、写入访问器
            Object.defineProperty(target, key, {
                get: function () {
                    return _val;
                },
                set: function (newVal) {
                    _val = newVal;
                },
                enumerable: true,
                configurable: true
            });
        }
    };

    var index = { globalConfig: globalConfig, ignore: ignore, createStore: createStore, StoreBase: StoreBase };

    exports.globalConfig = globalConfig;
    exports.ignore = ignore;
    exports.createStore = createStore;
    exports.StoreBase = StoreBase;
    exports.default = index;

    Object.defineProperty(exports, '__esModule', { value: true });

})));
//# sourceMappingURL=matrox.umd.js.map
